# Agent Instructions

- After implementing a fix for this app, run `make run` locally to verify the app launches and the changed behavior can be exercised. If `make run` cannot be completed, document the concrete blocker and the closest verification that was performed.
